version: 2

models:
  - name: dim_movie
    columns:
      - name: movie_id
        tests:
          - not_null
          - unique

  - name: dim_genre
    columns:
      - name: genre_id
        tests:
          - not_null
          - unique

  - name: dim_person
    columns:
      - name: person_id
        tests:
          - not_null
          - unique

  - name: fct_movie_daily_snapshot
    columns:
      - name: movie_id
        tests:
          - not_null
      - name: snapshot_date
        tests:
          - not_null

  - name: bridge_movie_genre
    columns:
      - name: movie_id
        tests:
          - not_null
      - name: genre_id
        tests:
          - not_null

  - name: bridge_movie_cast
    columns:
      - name: movie_id
        tests:
          - not_null
      - name: person_id
        tests:
          - not_null
  - name: dim_company
    columns:
      - name: company_id
        tests:
          - not_null
          - unique

  - name: dim_keyword
    columns:
      - name: keyword_id
        tests:
          - not_null
          - unique

  - name: dim_country
    columns:
      - name: country_code
        tests:
          - not_null
          - unique

  - name: dim_language
    columns:
      - name: language_code
        tests:
          - not_null
          - unique

  - name: bridge_movie_crew
    columns:
      - name: movie_id
        tests:
          - not_null
      - name: person_id
        tests:
          - not_null